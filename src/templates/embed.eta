#pragma once

#include <array>
#include <cstdint>

namespace saucer::embedded
{
    static constexpr std::array<std::uint8_t, <%= it.size %>> <%= it.name %> = {
        <%= it.data %>
    };
} // namespace saucer::embedded
